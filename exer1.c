/*#include <stdio.h>
#include <math.h>

main() {
  // Criar um algoritmo que receba o valor de x, e calcule e imprima o valor de f(x). 

  float x;
  printf("Digite o valor de X: ");
  scanf("%f", &x);

  if( x * x - 16 >= 0){
    float resultado = ( 5 * x + 3) / sqrt(x * x - 16);
    printf("f(x) = %.2f\n", resultado);
  }
  else{
    printf("O valor de x não é valido.");
  }
}*/